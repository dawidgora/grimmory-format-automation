apiVersion: v1
kind: Service
metadata:
  name: grimmory-format-service
  labels:
    app.kubernetes.io/name: grimmory-format-service
    app.kubernetes.io/part-of: grimmory-format-automation
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: grimmory-format-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
